---
import Base from '../layouts/Base.astro';
import Post from '../components/BlogPost.astro';

interface BlogContent {
	description: string;
	featuredImage: string;
	featuredImageAlt: string;
	title: string;
	pubDate: string;
	updatedDate?: string;
	rssOnly?: boolean;
	draft?: boolean;
	atDid?: string;
}
export interface Props {
	url: string;
	content: BlogContent
}

const {
	content: { title, description, featuredImage, featuredImageAlt, pubDate, updatedDate, atDid},
	url,
} = Astro.props;
---

<Base
	title={title}
	description={description}
	permalink={`https://charlesvillard.co/blog/${url}`}
	featuredimage={featuredImage}
>
	{atDid && (
		<link
			slot="html-head"
			rel="site.standard.document"
  		href={atDid}
		/>
	)}
	<Post
		title={title}
		publishDate={pubDate}
		updatedDate={updatedDate}
		author="Charles Villard"
		authorLink="https://twitter.com/cdvillard"
		featuredImage={featuredImage}
		featuredImageAlt={featuredImageAlt}
	>
		<slot />
	</Post>
</Base>
